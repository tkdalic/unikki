import { TodoFocusDirective } from "./todo-focus.directive";

describe("TodoFocusDirective", () => {
  it("should create an instance", () => {
    const directive = new TodoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
